# 价格区交互优化说明

## ✅ 完成内容

优化了 Landing Page 价格区的按钮样式和选中交互效果。

---

## 🎨 主要修改

### 1. 按钮样式改为白色边框

**之前**：
- STARTER / BASIC / ULTIMATE：红色边框 (`btn-secondary`)
- PRO：青色填充 (`btn-primary`)

**现在**：
- 所有套餐：统一为白色边框按钮
- 鼠标悬停时：白色填充，文字变深色
- 样式：`border-2 border-text-primary text-text-primary`

**按钮效果**：
```tsx
<button 
  onClick={() => setSelectedPlan('starter')}
  className="w-full mb-6 px-8 py-3 bg-transparent border-2 border-text-primary text-text-primary hover:bg-text-primary hover:text-dark transition-all duration-300 rounded-sm"
>
  选择方案
</button>
```

### 2. 添加选中状态交互

**功能**：
- 点击任意"选择方案"按钮时，该套餐卡片显示**绿色边框**
- 未选中的卡片没有绿色边框
- 可以切换选中不同套餐

**实现方式**：
1. 添加状态管理：`const [selectedPlan, setSelectedPlan] = useState<string | null>(null)`
2. 点击按钮时更新状态：`onClick={() => setSelectedPlan('starter')}`
3. 根据状态动态添加边框类名：`${selectedPlan === 'starter' ? 'border-primary' : ''}`

---

## 📝 代码变化

### 状态管理（新增）

```typescript
const [selectedPlan, setSelectedPlan] = useState<string | null>(null) // 选中的套餐
```

### 卡片边框（4个卡片）

**Starter**：
```tsx
<div className={`card group hover:border-primary/50 transition-all duration-300 ${selectedPlan === 'starter' ? 'border-primary' : ''}`}>
```

**Basic**：
```tsx
<div className={`card group hover:border-primary/50 transition-all duration-300 ${selectedPlan === 'basic' ? 'border-primary' : ''}`}>
```

**Pro**：
```tsx
<div className={`card border-primary/50 group hover:border-primary transition-all duration-300 relative ${selectedPlan === 'pro' ? 'border-primary' : ''}`}>
```

**Ultimate**：
```tsx
<div className={`card group hover:border-primary/50 transition-all duration-300 ${selectedPlan === 'ultimate' ? 'border-primary' : ''}`}>
```

### 按钮样式（4个按钮）

所有按钮统一样式：
```tsx
<button 
  onClick={() => setSelectedPlan('starter')}  // 不同套餐ID
  className="w-full mb-6 px-8 py-3 bg-transparent border-2 border-text-primary text-text-primary hover:bg-text-primary hover:text-dark transition-all duration-300 rounded-sm"
>
  选择方案
</button>
```

---

## 🎯 效果预览

### 默认状态
- 所有卡片：深色背景，灰色边框
- 所有按钮：白色边框，透明背景

### 鼠标悬停
- 卡片：边框变为青色半透明
- 按钮：白色填充，深色文字

### 点击选中
- 选中的卡片：**绿色边框**（`border-primary`）
- 未选中的卡片：保持原样

---

## 🎨 颜色方案

| 元素 | 颜色 | CSS 类 |
|------|------|--------|
| 按钮边框（默认） | 白色 | `border-text-primary` |
| 按钮文字（默认） | 白色 | `text-text-primary` |
| 按钮背景（悬停） | 白色 | `hover:bg-text-primary` |
| 按钮文字（悬停） | 深色 | `hover:text-dark` |
| 卡片边框（选中） | 青绿色 | `border-primary` |
| 卡片边框（悬停） | 青绿色半透明 | `hover:border-primary/50` |

---

## 💡 交互流程

```
用户点击"选择方案"
       ↓
触发 onClick 事件
       ↓
setSelectedPlan('starter')
       ↓
更新 selectedPlan 状态
       ↓
React 重新渲染
       ↓
选中卡片添加 border-primary 类
       ↓
显示绿色边框 ✓
```

---

## 🚀 查看效果

1. 启动前端：
   ```bash
   cd frontend
   npm run dev
   ```

2. 打开浏览器：
   ```
   http://localhost:5173
   ```

3. 滚动到"订阅计划与价格"区域

4. 测试交互：
   - ✅ 点击任意"选择方案"按钮
   - ✅ 该卡片显示绿色边框
   - ✅ 点击其他按钮，边框会切换
   - ✅ 鼠标悬停在按钮上，背景变白

---

## ✨ 优化点

1. **视觉统一**：所有按钮样式统一为白色边框
2. **交互明确**：选中状态有清晰的视觉反馈（绿色边框）
3. **用户友好**：可以随时切换选择不同套餐
4. **动画流畅**：使用 `transition-all duration-300` 实现平滑过渡

---

## 🎉 完成！

价格区的交互已优化完成：
- ✅ 按钮改为白色边框样式
- ✅ 点击后显示绿色边框选中效果
- ✅ 可以切换选中不同套餐
- ✅ 动画流畅，用户体验良好

现在价格区更加美观和易用！💚✨

