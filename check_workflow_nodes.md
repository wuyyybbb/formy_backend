# 如何查看 RunningHub 工作流的节点 ID

## 换背景工作流节点映射检查

当前配置的节点映射：
```yaml
model_image:  # 前端"原始图片"
  node_id: "1"
  field_name: "image"

bg_image:  # 前端"背景图片"
  node_id: "2"
  field_name: "image"
```

## ⚠️ 需要确认的信息

请在 RunningHub 后台（https://www.runninghub.ai/workflow/1996478152513830914）查看：

### 输入节点：
1. **input:model_image:1** (模特图片) 
   - 实际节点编号是多少？（比如 #1, #3, #7 等）
   - 字段名称是什么？（比如 "image", "input_image" 等）

2. **input:bg_image:2** (背景图片)
   - 实际节点编号是多少？
   - 字段名称是什么？

### 输出节点：
3. **output:image:1** (生成结果)
   - 实际节点编号是多少？（比如 #4, #10 等）

4. **output:image_comparer:2** (对比图)
   - 实际节点编号是多少？（比如 #11 等）

## 📝 示例

假设在 RunningHub 工作流编辑器中看到：
- 节点 #3: LoadImage (input:model_image:1)
- 节点 #7: LoadImage (input:bg_image:2)
- 节点 #10: SaveImage (output:image:1)
- 节点 #11: ImageComparer (output:image_comparer:2)

那么配置应该是：
```yaml
node_mapping:
  model_image:
    node_id: "3"  # ⚠️ 注意：这里应该是 "3" 而不是 "1"
    field_name: "image"
  bg_image:
    node_id: "7"  # ⚠️ 注意：这里应该是 "7" 而不是 "2"
    field_name: "image"
```

## 🔍 如何查看节点编号

1. 打开工作流页面
2. 点击每个节点
3. 查看节点左上角的编号（通常显示为 #数字）
4. 确认输入/输出节点的实际编号

请将正确的节点编号告诉我，我会更新配置！

